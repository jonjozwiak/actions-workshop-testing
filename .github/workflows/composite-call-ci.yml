name: Composite CI

on:
  push:
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-and-install
        with:
          node-version: '20'
      - run: npm test

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/setup-and-install
        with:
          node-version: '20'
      - run: npm run lint